import rss from '@astrojs/rss'; import {getCollection} from 'astro:content'; export async function GET(context){const entries=await getCollection('writing',({data})=>!data.draft);return rss({title:'Jaehee’s Garden',description:'게임과 책, 생각이 자라는 개인 정원',site:context.site,items:entries.map(e=>({title:e.data.title,description:e.data.description,pubDate:e.data.published,link:`/${e.data.type==='essay'?'essays':'notes'}/${e.id}/`}))})}
