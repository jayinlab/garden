---
const { entry } = Astro.props;
---
<a class="card" href={`/essays/${entry.id}`}>
  <span class="card-type">Essay</span>
  <h3>{entry.data.title}</h3>
  <p>{entry.data.description}</p>
  <time>{entry.data.published.toLocaleDateString('ko-KR')}</time>
</a>
